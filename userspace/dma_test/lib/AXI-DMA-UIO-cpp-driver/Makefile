# build test.cpp using g++ compiler and -std=c++11 flag
# and output the executable to bin/test

all: 
	rm -rf build
	mkdir build
	g++ -std=c++11 src/test.cpp -o build/test -Iinclude/

clean:
	rm -rf build